import React from 'react';
import { HeroContainer, SubTitle, CircleBackground } from './hero.styles';

const Hero = () => {
	return (
		<HeroContainer data-scroll-section>
			<CircleBackground />
		</HeroContainer>
	);
};

export default Hero;
